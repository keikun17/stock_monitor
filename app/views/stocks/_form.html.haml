= form_for @stock, :html => { :class => "form-horizontal" } do |f|
  -if @stock.errors.any?
    .alert.alert-danger.alert-dismissable
      %button.close{"aria-hidden" => "true", "data-dismiss" => "alert", :type => "button"} &times;
      %h4= "#{pluralize(@stock.errors.count, "error")} prohibited this stock from being saved:"

      %ul
        - @stock.errors.full_messages.each do |msg|
          %li= msg

  .form-group
    = f.label :stock_template, :class => 'col-sm-2 control-label'
    .col-sm-10
      = f.text_field :stock_template, :class => 'form-control'
  .form-group
    .col-sm-offset-2.col-sm-10
      = f.submit :class => 'btn btn-primary'
